# 365Quest
365天的挑戰

下載專案後，通常只要在專案目錄執行：

```
npm install
npm start
```

就會先把缺的 node_modules 補齊，再啟動程式。

如果要打包執行檔，再執行：

```
npm run build:win
```
或
```
npm run build:mac
```

## Release
有打包了windows用的版本，mac的請自行打包或直接npm執行
